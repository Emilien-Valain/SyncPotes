import SyncPotesApp from "@/components/SyncPotesApp";

// The self-contained design showcase: all eight screens driven by demo data,
// no backend required. The real app lives at / and /p/[slug].
export default function DemoPage() {
  return <SyncPotesApp />;
}
